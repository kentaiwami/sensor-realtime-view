apiVersion: v1
kind: Service
metadata:
  name: sensor-realtime-view
  namespace: sensor-realtime-view
spec:
  type: NodePort
  selector:
    app: sensor-realtime-view
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32226
